export type AppTheme = {
  LoadApp: {
    Background: string
    loading: {
      Green: string
      red: string
    }
  }
  media: {
    xs: number
    sm: number
    md: number
    lg: number
  }
}
